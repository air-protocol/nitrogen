---
  kind: "Service"
  apiVersion: "v1"
  metadata:
    name: peernode
  spec:
    type: NodePort
    selector:
      app: value_mesh
    ports:
      - protocol: TCP
        port: 80
        targetPort: 80
